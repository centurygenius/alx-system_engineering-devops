#Install the "ufw"
sudo apt update
sudo apt install ufw

#By default, "ufw" denies all incoming connections 
#and allows all outgoing connections

#Allow specific ports

sudo ufw allow 22/tcp
sudo ufw allow 443/tcp
sudo ufw allow 80/tcp

#Enable "ufw" to activate the firewall with the configured rules.

sudo ufw enable

